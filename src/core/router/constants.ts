const CONST_MODES_ROUTER = Object.freeze({
  HASH: 'hash',
  HISTORY: 'history',
});

const CONST_MODES_SCHEDULING = Object.freeze({
  MACRO: 'macro',
  MICRO: 'micro',
  SYNC: 'sync',
});

export { CONST_MODES_ROUTER, CONST_MODES_SCHEDULING };
